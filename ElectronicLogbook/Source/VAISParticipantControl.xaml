<UserControl x:Class="ElectronicLogbook.VAISParticipantControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d">
    <DockPanel DataContext="{Binding mVAISParticipantListViewModel}">
        <ListView ItemsSource="{Binding}">
            <ListView.View >
                <GridView >
                    <GridViewColumn Header="Participant Name" DisplayMemberBinding="{Binding mParticipantName}"  Width="Auto" />
                    <GridViewColumn Header="Participant Part Number"  DisplayMemberBinding="{Binding mParticipantPartNumber}" Width="Auto" />
                    <GridViewColumn Header="Participant Version Number"  DisplayMemberBinding="{Binding mParticipantVersionNumber}" Width="Auto" />
                    <GridViewColumn Header="Participant Description"  DisplayMemberBinding="{Binding mParticipantDescription}" Width="Auto" />
                    <GridViewColumn Header="Participant Location"  DisplayMemberBinding="{Binding mParticipantLocation}" Width="Auto" />
                </GridView>
            </ListView.View>
        </ListView>
    </DockPanel>
</UserControl>
